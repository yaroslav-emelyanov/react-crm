export enum AppPaths {
  categories = '/categories',
  detailRecord = '/detail-record',
  history = '/history',
  home = '/',
  login = '/login',
  planning = '/planning',
  profile = '/profile',
  record = '/record',
  register = '/register',
}

export enum Rates {
  EUR = 'EUR',
  RUB = 'RUB',
  USD = 'USD',
}

export enum RecordTypes {
  income = 'income',
  outcome = 'outcome',
}
