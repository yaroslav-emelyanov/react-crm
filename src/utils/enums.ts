export enum AppPath {
  categories = '/categories',
  detailRecord = '/detail-record',
  history = '/history',
  home = '/',
  login = '/login',
  planning = '/planning',
  profile = '/profile',
  record = '/record',
  register = '/register',
}

export enum Rate {
  EUR = 'EUR',
  RUB = 'RUB',
  USD = 'USD',
}
